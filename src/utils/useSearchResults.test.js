// Local modules.
import useSearchResults from './useSearchResults';

// -----------------------------------------------------------------------------

describe('useSearchResults', () => {
  test('is a function', () => {
    expect(typeof useSearchResults).toEqual('function');
  });

  // TODO: Write <useSearchResults /> tests.
});
