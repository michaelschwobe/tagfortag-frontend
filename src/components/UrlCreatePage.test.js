import React from 'react';
import { shallow } from 'enzyme';

// Components.
import UrlCreatePage from './UrlCreatePage';

// -----------------------------------------------------------------------------

describe('<UrlCreatePage>', () => {
  test('shallow render', () => {
    shallow(<UrlCreatePage />);
  });

  // TODO: Write <UrlCreatePage /> tests.
});
