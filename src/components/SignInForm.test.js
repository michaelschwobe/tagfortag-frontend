import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import SignInForm from './SignInForm';

// -----------------------------------------------------------------------------

describe('<SignInForm>', () => {
  test('shallow render', () => {
    shallow(<SignInForm />);
  });

  // TODO: Write <SignInForm /> tests.
});
