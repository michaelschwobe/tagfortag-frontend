import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import Root from './Root';

// -----------------------------------------------------------------------------

describe('<Root>', () => {
  test('shallow render', () => {
    shallow(<Root />);
  });

  // TODO: Write <Root /> tests.
});
