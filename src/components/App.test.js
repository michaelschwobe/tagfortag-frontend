import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import App from './App';

// -----------------------------------------------------------------------------

describe('<App>', () => {
  test('shallow render', () => {
    shallow(<App />);
  });

  // TODO: Write <App /> tests.
});
