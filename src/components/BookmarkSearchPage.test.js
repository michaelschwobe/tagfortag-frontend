import React from 'react';
import { shallow } from 'enzyme';

// Components.
import BookmarkSearchPage from './BookmarkSearchPage';

// -----------------------------------------------------------------------------

describe('<BookmarkSearchPage>', () => {
  test('shallow render', () => {
    shallow(<BookmarkSearchPage />);
  });

  // TODO: Write <BookmarkSearchPage /> tests.
});
