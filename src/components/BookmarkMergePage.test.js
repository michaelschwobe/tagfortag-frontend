import React from 'react';
import { shallow } from 'enzyme';

// Components.
import BookmarkMergePage from './BookmarkMergePage';

// -----------------------------------------------------------------------------

describe('<BookmarkMergePage>', () => {
  test('shallow render', () => {
    shallow(<BookmarkMergePage />);
  });

  // TODO: Write <BookmarkMergePage /> tests.
});
