import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import SearchResultsHelp from './SearchResultsHelp';

// -----------------------------------------------------------------------------

describe('<SearchResultsHelp>', () => {
  test('shallow render', () => {
    shallow(<SearchResultsHelp />);
  });

  // TODO: Write <SearchResultsHelp /> tests.
});
