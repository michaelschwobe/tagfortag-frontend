import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import TagCreatePage from './TagCreatePage';

// -----------------------------------------------------------------------------

describe('<TagCreatePage>', () => {
  test('shallow render', () => {
    shallow(<TagCreatePage />);
  });

  // TODO: Write <TagCreatePage /> tests.
});
