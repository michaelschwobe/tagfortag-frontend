import React from 'react';
import { shallow } from 'enzyme';

// Components.
import UrlUpdatePage from './UrlUpdatePage';

// -----------------------------------------------------------------------------

describe('<UrlUpdatePage>', () => {
  test('shallow render', () => {
    shallow(<UrlUpdatePage />);
  });

  // TODO: Write <UrlUpdatePage /> tests.
});
