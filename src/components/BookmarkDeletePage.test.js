import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import BookmarkDeletePage from './BookmarkDeletePage';

// -----------------------------------------------------------------------------

describe('<BookmarkDeletePage>', () => {
  test('shallow render', () => {
    shallow(<BookmarkDeletePage />);
  });

  // TODO: Write <BookmarkDeletePage /> tests.
});
