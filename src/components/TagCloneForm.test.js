import React from 'react';
import { shallow } from 'enzyme';

// Components.
import TagCloneForm from './TagCloneForm';

// -----------------------------------------------------------------------------

describe('<TagCloneForm>', () => {
  test('shallow render', () => {
    shallow(<TagCloneForm />);
  });

  // TODO: Write <TagCloneForm /> tests.
});
