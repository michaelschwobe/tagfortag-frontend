import React from 'react';
import { shallow } from 'enzyme';

// Components.
import BookmarkUpdatePage from './BookmarkUpdatePage';

// -----------------------------------------------------------------------------

describe('<BookmarkUpdatePage>', () => {
  test('shallow render', () => {
    shallow(<BookmarkUpdatePage />);
  });

  // TODO: Write <BookmarkUpdatePage /> tests.
});
