import React from 'react';
import { shallow } from 'enzyme';

// Components.
import UrlMergePage from './UrlMergePage';

// -----------------------------------------------------------------------------

describe('<UrlMergePage>', () => {
  test('shallow render', () => {
    shallow(<UrlMergePage />);
  });

  // TODO: Write <UrlMergePage /> tests.
});
