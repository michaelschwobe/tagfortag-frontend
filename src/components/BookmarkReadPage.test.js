import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import BookmarkReadPage from './BookmarkReadPage';

// -----------------------------------------------------------------------------

describe('<BookmarkReadPage>', () => {
  test('shallow render', () => {
    shallow(<BookmarkReadPage />);
  });

  // TODO: Write <BookmarkReadPage /> tests.
});
