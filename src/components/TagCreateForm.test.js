import React from 'react';
import { shallow } from 'enzyme';

// Local modules.
import TagCreateForm from './TagCreateForm';

// -----------------------------------------------------------------------------

describe('<TagCreateForm>', () => {
  test('shallow render', () => {
    shallow(<TagCreateForm />);
  });

  // TODO: Write <TagCreateForm /> tests.
});
